spring:
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: test
  data:
    mongodb:
      database: test

de:
  flapdoodle:
    mongodb:
      embedded:
        version: "6.0.2"

logging:
  level:
    ROOT: WARN
