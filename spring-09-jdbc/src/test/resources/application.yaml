#server:
#  error:
#    whitelabel:
#      enabled: false
#    include-stacktrace: always
#    include-message: always
#    path: /err
spring:
  mvc:
    throw-exception-if-no-handler-found: true
#  web:
#    resources:
#      add-mappings: false
#  messages:
#    basename: i18n/messages
#    encoding: UTF-8
  data:
    mongodb:
      database: librarydb
      port: 0 # when flapdoodle using
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2
mongock:
  runner-type: "InitializingBean" # For Spring Shell.
  change-logs-scan-package:
    - ru.otus.spring.mongock.changelog_test
  mongo-db:
    write-concern:
      journal: false
    read-concern: local
logging:
  level:
    ROOT: WARN
