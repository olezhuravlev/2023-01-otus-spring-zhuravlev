spring:
  shell:
    interactive:
      enabled: false
    noninteractive:
      enabled: true
    command:
      version:
        enabled: false
  integration:
    management:
      default-logging-enabled: true

logging:
  level:
    root: info
    sun: info
    javax: info
