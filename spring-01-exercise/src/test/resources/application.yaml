spring:
  messages:
    basename: i18n/messages
    encoding: UTF-8
  shell:
    interactive:
      enabled: false

application:
  autorun: false
  questions-path: questions.csv
  success-threshold: 70
  logger-enabled: false
